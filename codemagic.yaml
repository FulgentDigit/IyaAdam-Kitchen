workflows:
  android-workflow:
    name: Android Build
    max_build_duration: 120
    triggering:
      events:
        - push
    cache:
      cache_paths: []
    scripts:
      - name: Build APK
        script: |
          gradle clean assembleDebug
    artifacts:
      - app/build/outputs/apk/debug/app-debug.apk
